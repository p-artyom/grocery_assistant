server {
  listen 80;
  server_tokens off;
  server_name 158.160.15.128;
  client_max_body_size 20M;

  location /api/ {
    proxy_pass http://backend:9000/api/;
  }

  location /admin/ {
    proxy_pass http://backend:9000/admin/;
  }

  location /api/docs/ {
    alias /static/;
    try_files $uri $uri/redoc.html;
  }

  location /media/ {
    alias /app/media/;
  }

  location / {
    alias /static/;
    try_files $uri /index.html;
  }
}
